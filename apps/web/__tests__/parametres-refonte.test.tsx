// Superseded by parametres-r5.test.tsx (R5 — 8 tabs).
// Kept as a stub so the file reference doesn't 404 in git history.
describe('ParametresPage — R4 (superseded)', () => {
  it.skip('was replaced by parametres-r5 tests', () => {});
});
